# Backend/Dockerfile

# Use Node.js LTS
FROM node:18-alpine

# Create app directory
WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install --production

# Copy all backend files
COPY . .

# Expose port
EXPOSE 3000

# Start app
CMD ["node", "server.js"]
